# Supabase (optional — app works offline with IndexedDB if unset)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Inference API (optional — uses client mock if unset)
VITE_INFERENCE_API_URL=http://localhost:8000

# AI Assistant API (optional — uses local deterministic responder if unset)
VITE_ASSISTANT_API_URL=
