fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
httpx==0.28.1
python-dotenv==1.0.1

# Real audio model stack (optional — the service degrades to a deterministic
# mock if these are missing). CPU torch keeps the image small.
torch
transformers
librosa
soundfile
numpy
